{
  "name": "victor-sotero-portfolio",
  "version": "1.0.0",
  "description": "Portfolio website and CV for Victor Sotero - Senior Data Engineer",
  "scripts": {
    "dev": "hugo server -D --disableFastRender",
    "build": "hugo --gc --minify",
    "generate:cv-variants": "python3 scripts/generate_cv_variants.py",
    "test:timeline": "npm run build && node scripts/assert-experience-timeline.js",
    "format": "prettier --write \"layouts/**/*.html\" \"static/**/*.{css,js}\"",
    "format:check": "prettier --check \"layouts/**/*.html\" \"static/**/*.{css,js}\""
  },
  "author": "Victor Sotero",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.2.0"
  }
}
